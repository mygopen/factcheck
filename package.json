{
  "name": "factcheck-slack-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "draft": "node scripts/generate-draft.mjs",
    "check": "node --check src/worker.js && node --check scripts/generate-draft.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
